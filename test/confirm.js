import { expect } from 'chai';

describe('Confirm Tests are working', () => {
    it('should return the ultimate question of life, the universe, and everything', () => {
        expect(40 + 2).to.equal(42);
    });
});
